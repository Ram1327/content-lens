// Re-exports from @content-lens/shared-types for convenient import within apps/web.
// Import from here instead of the package directly when inside src/.
export type { Verdict, DetectTextRequest, DetectTextResponse } from "@content-lens/shared-types";
